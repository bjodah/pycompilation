when:
  - event: [push]

steps:
  - name: build-and-test
    image: cont-reg.bjodah.se:443/bjodah/triceratops-4:23
    environment:
      - PYTHONMALLOC=malloc
    commands:
      - bash -l ./scripts/ci.sh ${CI_REPO_NAME}
      - ./scripts/generate_docs.sh
      - ./scripts/prepare_deploy.sh
